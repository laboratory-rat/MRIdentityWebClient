import list from './list';
import updateRoles from './updateRoles';

const namespaced = true;
const state = {};
const getters = {};
const mutations = {};
const actions = {};
const modules = {
    list,
    updateRoles
}

export default {
    namespaced,
    state,
    getters,
    mutations,
    actions,
    modules
}