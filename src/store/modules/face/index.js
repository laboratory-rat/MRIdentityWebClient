import list from './list';
import selected from './selected';

const state = {};
const getters = {};
const mutations = {};
const actions = {};
const modules = {
    list,
    selected,
}

export default {
    namespaced: true,
    state, 
    getters,
    mutations,
    actions,
    modules
}