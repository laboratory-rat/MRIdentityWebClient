import profile from './profile';
import password from './password';
import avatar from './avatar';

const state = {};
const getters = {};
const mutations = {};
const actions = {};
const modules = {
    profile,
    password,
    avatar
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions,
    modules
}